name: sportin
description: A new Flutter project.

dev_dependencies: 
  flutter_test:
    sdk: flutter
    
  #flutter_launcher_icons: "^0.1.0"
  
flutter_icons:
  image_path: "assets/ic_launcher.jpg" 
  android: true
  ios: true
dependencies:
  flutter:
    sdk: flutter
  shared_preferences: "^0.5.2"
  intl: "^0.15.8"
  numberpicker: "^1.0.0"
  image_picker: "^0.6.0+1"
  http: "^0.12.0+2"
  crypto: "^2.0.6"
  #jaguar_jwt: "^1.1.5"
  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: "^0.1.2"


# For information on the generic Dart part of this file, see the
# following page: https://www.dartlang.org/tools/pub/pubspec

# The following section is specific to Flutter.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/icon.jpg
    - assets/logo1.png
    - assets/logo2.png
    - assets/jugador.jpg
    - assets/Welcome_photo.jpg
    - assets/LOGO_SPORTIN.jpg
    - assets/icono_1.png
    - assets/nacionalidad.png
    - assets/localidad.png
    - assets/correo.png
    - assets/fecha.png
    - assets/sexo.png
    - assets/pierna.png
    - assets/peso.png
    - assets/altura.png
    - assets/historial.png
    - assets/bio.png
    - assets/prueba_jugador.png
    - assets/prueba_entrenador.png
    - assets/prueba_director.png
    - assets/prueba_club.png
    - assets/prueba_jugador_2.png
    - assets/prueba_entrenador_2.png
    - assets/prueba_director_2.png
    - assets/prueba_club_2.png
    - assets/logo_me_interesa.png

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.io/assets-and-images/#resolution-aware.

  # For details regarding adding assets from package dependencies, see
  # https://flutter.io/assets-and-images/#from-packages

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies, 
  # see https://flutter.io/custom-fonts/#from-packages
  